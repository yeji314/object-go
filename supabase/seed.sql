-- Seed data (optional). Run manually after creating an admin user.
-- This file is kept minimal so you can adapt it to your environment.

-- Example: promote a user to admin after they signed up via email/password.
-- Replace the email below with your own admin email, then run this in Supabase SQL editor.
-- update public.profiles set role = 'admin' where email = 'admin@example.com';
